# Project Instructions

<!-- repository-creator:managed:start -->

- 作業前に依頼と現在の実装を確認し、存在する文書のうち関係するものだけを読む。
- 新規サービスの一気通貫開発、またはコア完成後の品質向上・拡充では、最初に `WORKFLOW.md` を読む。工程を圧縮せず、前段階の完了確認後に次へ進む。
- 機能、フロー、状態、ルール、データ、外部連携を扱う場合は `FEATURES.md` を読む。
- サービス固有のコンテンツ、その範囲・深さ・構造・品質、ユーザー向け文言を扱う場合は `CONTENT.md` を読む。
- Design / UI / UX、視覚表現、操作方法、実在プロダクトのデザイン参照を扱う場合は、実装前に `DESIGN.md` を読む。
- ユーザー向けデザインの実装完了後に独立デザインレビューを行う場合は `DESIGN_REVIEW.md` を読む。制作時の常設ルールとしては使わない。
- 複数領域にまたがる場合は該当文書を併読する。何が起きるかは `FEATURES.md`、どんな中身を提供するかは `CONTENT.md`、どう見せて操作させるかは `DESIGN.md` の責務とする。
- 関係しない文書は読まず、存在しない専門文書を空のまま追加しない。
- 恒常的な判断が変わった場合だけ、該当する専門文書の管理マーカー外にあるプロジェクト固有部分を更新する。管理マーカー内は編集しない。
- 確認は変更内容に応じた必要最小限とする。Desktop / Mobileの両方で一連の操作を通す網羅的な検証をデフォルトでは行わない。デザイン作業では必要に応じて実画面やスクリーンショットで見た目を確認する。
- ローカルとGitHubの全ファイルをハッシュで照合したり、完全一致を確認したりする作業はデフォルトでは行わない。競合、同期不整合、反映漏れが疑われる場合だけ必要な範囲で確認する。
- リポジトリ構成、依存関係、恒常文書は必要最小限に保つ。

<!-- repository-creator:managed:end -->

## このプロジェクトの範囲

- このリポジトリは、複数の架空事業を進める起業シミュレーターとして使う。
- 起業に必要なタスクを管理し、事業内容が決まったらAIが全項目の成果物を一括で仮作成する。その後は、ユーザーが選んだ項目をAIと1つずつ修正できる。
- 共通タスクテンプレートは `templates/tasks.md` を正本とする。
- 新規事業は `ventures/<business-id>/` に作り、`templates/tasks.md` と `templates/decisions.md` をその事業に複製して開始する。
- 各事業のタスクは `tasks.md`、承認内容は `decisions.md` に記録し、素材・ホームページを含む成果物は同じ事業フォルダ直下に保存する。`artifacts/`・`assets/`・`website/` は作らず、他事業の内容を混在させない。

## 実行モードと優先順位

実行モードの正本はこの節とする。READMEは使い方の説明、`templates/tasks.md` と各事業の `tasks.md` は成果物の要件を定義する。

1. ユーザーが明示した範囲・停止・進め方を優先する。「今回は事業決定だけ」「1つずつ進めたい」などの指定があれば個別実行とする。
2. 新規事業の内容が入力されたら、一括仮作成を標準とする。「まずは事業を決めます」「京都で小さなサロン向けのインテリアコーディネート」のような入力は、その開始条件を満たす。「まずは」という語だけで個別実行や終了を推定しない。
3. 既存事業について全項目の仮作成・続きを求められたら、保存済みの内容を引き継いで一括仮作成する。既存事業の特定項目の作成・修正依頼は個別実行とする。一覧表示・質問・ルール編集だけの依頼から事業の成果物作成を開始しない。
4. 一括仮作成では、タスク本文の入力・選択・確認・承認待ちより、この節の「待たずに仮決めする」規定を優先する。成果物の内容・調査・品質要件は省略しない。

既存事業に複製済みのタスク表にもこの判定を適用する。旧形式の「選択時」に確認・承認手順が含まれていても、一括仮作成の停止条件にはしない。既存の成果物や承認内容は保持し、テンプレートで事業固有のタスク表を上書きしない。

## 基本フロー

### 一括仮作成

1. ユーザーが入力した事業内容を採用し、対象事業のタスク表・決定事項・既存成果物を確認する。新規事業は所定のフォルダ作成・命名手順で開始する。
2. ユーザーがすでに決めた条件を使用し、未指定事項はAIが仮決めする。商圏が未指定、または「京都」のように市・府の区別がなければ京都市を仮採用し、確認待ちにしない。別の商圏が指定されていればその指定を使う。
3. 全タスクの成果物を作成する。事業名の選択、料金の承認、HPのデザイン確認、SNSの選択などで停止せず、必要な選択をAIが行う。計画や候補一覧だけで終わらず、タスクが求める画像・料金表・HP・フォームなどの実物まで作る。
4. 完成した成果物を保存し、対応するタスクをチェックONにする。AIの仮設定とユーザーが決めた内容は、下記の記録ルールに従って区別する。
5. 全項目の成果物を保存し、今回の変更をGitHubへ反映・確認したうえで、成果物一覧とリンク、主な仮設定をまとめて提示して終了する。その後の修正項目はユーザーが選ぶ。

- 1項目の完成やユーザー承認待ちを理由に一括仮作成を終了しない。途中で中断された場合は、保存済みの成果物とチェック状態から再開し、完成済みの内容を重複作成しない。
- 実際のツール制約・エラー等で作れない項目はチェックOFFを保ち、可能な他項目を進める。最後に未完了項目・理由・必要な対応を示し、「全項目完成」とは報告しない。GitHubに反映できなかった場合も未反映であることを明記する。
- 一括仮作成はシミュレーション成果物の作成を指す。各タスクにある実アカウント作成・申請を行わない等の条件は引き続き守る。

### 個別実行

1. ユーザーが指定した事業・項目だけを作業する。
2. 対象タスクの「個別実行時」に従って、必要な入力・選択・確認を受ける。旧形式の「選択時」も個別実行の手順として読む。
3. 成果物・決定事項・タスク状態を更新し、今回の変更をGitHubへ反映・確認してリンクを提示する。
4. 次の未完了タスクへは進まず、ユーザーが次の項目を選ぶまで待つ。

## タスク管理ルール

- タスク状態はチェックボックスのOFF/ONだけで管理する。
- 未完了は `- [ ]`、完了は `- [x]` とする。
- 「進行中」「確認待ち」「不要」など、別の状態は追加しない。
- チェックONは成果物の完成を表し、ユーザーの承認済みを意味しない。仮設定であっても必要な成果物が完成していればONにする。ユーザーが完了と判断した場合もONにできるが、実際には存在しない成果物を作成済みとは報告しない。
- 不要なタスクは対象事業の `tasks.md` から削除する。
- その事業に必要な固有タスクは対象事業の `tasks.md` に追加する。
- 共通テンプレート自体は、ユーザーから明示的に変更依頼があった場合だけ変更する。

## 決定事項の記録

- 各事業の `decisions.md` 内を「ユーザーが決めた内容」と「AIの仮設定」に分ける。新しい管理ファイルやタスク状態は増やさない。
- ユーザーが入力・承認した内容は前者へ、AIが選んだ屋号・商圏・料金等の未承認の設定は後者へ記録する。成果物の完成だけで仮設定を承認済みにしない。
- ユーザーが仮設定を承認・修正したら、対象の内容を前者へ移し、後者に古い内容を残さない。承認日や既存の記録は保持する。
- 既存事業の記録を更新するときは、根拠を確認できた内容から区別する。過去の記録を推測で承認済みに変更しない。

## 作業ルール

- 毎回のタスク開始前に、GitHub上の最新のAGENTS.mdと関係する文書・対象事業のファイルを確認する。ローカルと差がある場合は未反映の編集を保持して差分を確認し、古いルールだけで進めない。GitHubを確認できなければ、確認できた版と制約を伝える。
- 通常のチャット・サイドチャットを問わず、ユーザーがタスクを承認または完了としたら、対象事業フォルダ内の該当する成果物・承認記録・タスク状態を更新し、今回の変更をGitHubへ反映する。反映を確認したうえで更新したファイルへのリンクを報告する。反映できなければ、未反映であることとその原因を伝える。
- チャットの種類にかかわらず、ユーザーが承認した内容は対象事業の `decisions.md` に随時記録・更新する。
- タスクの実作業が完了したら、成果物を保存し、対象タスクを `- [x]` に更新する。
- タスクはユーザーと合意したものだけを追加する。一括仮作成では既存タスクの成果物に必要な未設定事項を仮決めする。個別実行では対象タスクの手順とユーザーの指定範囲に従う。
- ユーザーから頼まれていない機能、タスク、仕様を勝手に増やさない。
- 共通ルールはルート、事業固有の内容は対象事業内に置く。必要になるまでファイルや下位フォルダを増やさない。

## 事業フォルダの命名と変更

- 新規事業は未使用の管理用ID `venture-001`、`venture-002` のような名前で開始する。既存フォルダを上書きしない。
- 「事業を決める」で事業内容が確定したら、AIが内容を表す短い英小文字のフォルダ名を決める。複数語はハイフンで区切る（例：カウンセリング事業 → `counseling`）。
- そのタスクの完了処理で、`ventures/<管理用ID>/` を `ventures/<事業内容を表す名前>/` に一度変更する。
- 同名の別事業が存在する場合は `counseling-002`、`counseling-003` のように、002から順に未使用の番号を付ける。既存の別事業へ統合・上書きしない。
- フォルダ内のタスク、成果物、完了チェック、決定事項を保持して移動し、READMEやリポジトリ内の旧パスへの参照を更新する。移動後に相対リンクが有効であることも確認する。
- 最新状態を読み、移動・参照更新を同じコミットで反映する。他の更新と競合した場合は読み直す。変更済みの対象事業を再実行しても、再改名や別事業の複製をしない。
- 後から「事業名を決める」で屋号が決まっても、フォルダ名は変更しない。以後の改名はユーザーから明示的な依頼がある場合だけ行う。
- 反映後は新しいフォルダ名とタスク表へのリンクを報告する。
